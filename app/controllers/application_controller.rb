class ApplicationController < ActionController::Base
  # before_action :authenticate_user!

  # private

  # def authenticate_user!
  #   redirect_to new_customer_session_path unless customer_signed_in? || admin_signed_in?
  # end
end
