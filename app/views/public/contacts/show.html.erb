<div class="row mb-3">
  <table class="table table-bordered custom-table">
    <tr>
      <td><%= @contact.title %></td>
    </tr>
    <tr>
      <td><%= @contact.created_at.strftime("%Y-%m-%d %H:%M:%S") %><br><%= @contact.body %></td>
    </tr>
  </table>
</div>

<div class="row mb-3">
  <div class="col-sm d-flex justify-content-center">
    <%= link_to "一覧", contacts_path, class: "btn btn-dark" %>
  </div>
</div>
